import 'package:flutter/material.dart';

class HelperGuideScreen extends StatelessWidget {
  const HelperGuideScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Helper Guide'),
      ),
      body: Container(),
    );
  }
}
